fastapi
uvicorn[standard]
python-multipart
python-dotenv
sqlalchemy
psycopg2-binary
faster-whisper
aiofiles
pyannote.audio
torch
torchaudio
pyjwt[crypto]
# Voice activity detection (github.com/snakers4/silero-vad) — see vad.py.
silero-vad
# Cosine clustering for the WeSpeaker re-clustering pass — see speaker_merge.py.
# (pyannote.audio pulls this in too; listed because we import it directly.)
scikit-learn
# Noise suppression is DeepFilterNet3 (github.com/Rikorose/DeepFilterNet), but
# it is NOT a pip dependency: the package pins numpy<2 and needs a Rust
# toolchain, so we use the upstream prebuilt binary instead. Install it with
#   ./scripts/install_deep_filter.sh
# See denoise.py for the full reasoning.
scipy
# The small-talk filter (relevance.py) runs a fine-tuned deberta-v3-small
# locally. sentencepiece is DeBERTa-v3's tokenizer and is not optional.
transformers
sentencepiece
# PDF export for the generated SOPs (sop.py). Optional: without it the .txt
# download still works and /health reports "sop_pdf": false.
reportlab
# Training-only, for build_dataset.py / finetune.py. Leave them out of a
# deployment that only ever loads an already-trained checkpoint.
datasets
accelerate

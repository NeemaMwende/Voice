fastapi
uvicorn[standard]
python-multipart
python-dotenv
sqlalchemy
psycopg2-binary
faster-whisper
aiofiles
pyannote.audio
torch
torchaudio
# Voice activity detection (github.com/snakers4/silero-vad) — see vad.py.
silero-vad
# Cosine clustering for the WeSpeaker re-clustering pass — see speaker_merge.py.
# (pyannote.audio pulls this in too; listed because we import it directly.)
scikit-learn
# Noise suppression is DeepFilterNet3 (github.com/Rikorose/DeepFilterNet), but
# it is NOT a pip dependency: the package pins numpy<2 and needs a Rust
# toolchain, so we use the upstream prebuilt binary instead. Install it with
#   ./scripts/install_deep_filter.sh
# See denoise.py for the full reasoning.
scipy
